# For local development, Vite proxies /api to this Django server.
VITE_BACKEND_URL=http://127.0.0.1:8000

# Optional production override. Leave blank locally so the /api proxy is used.
# VITE_API_BASE_URL=https://api.example.com/api
