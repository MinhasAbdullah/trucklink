# Optional. Leave VITE_API_BASE_URL unset when using the Vite /api proxy locally.
VITE_BACKEND_URL=http://127.0.0.1:8000
VITE_API_BASE_URL=/api

# Optional explicit websocket endpoint. If omitted the app derives /ws/realtime/.
# VITE_WS_URL=ws://127.0.0.1:8000/ws/realtime/
