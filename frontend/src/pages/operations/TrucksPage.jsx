import React, { useEffect, useMemo, useState } from "react";
import { Edit3, Plus, RefreshCw, Search, Trash2, WandSparkles } from "lucide-react";
import AppHeader from "../../components/layout/AppHeader";
import TruckForm from "../../components/operations/TruckForm";
import MatchCard from "../../components/operations/MatchCard";
import LoadingState from "../../components/ui/LoadingState";
import { operationsApi } from "../../api/operations";
import { getApiErrorMessage } from "../../api/client";
import { useOperations } from "../../context/OperationsContext";

const TrucksPage = () => {
  const { trucks, loadTrucks, createTruck, updateTruck, deleteTruck, acceptMatch } = useOperations();
  const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const [search, setSearch] = useState("");
  const [showForm, setShowForm] = useState(false); const [editing, setEditing] = useState(null);
  const [matchingId, setMatchingId] = useState(null); const [matches, setMatches] = useState([]); const [acceptingId, setAcceptingId] = useState(null);
  const refresh = async () => { setLoading(true); setError(""); try { await loadTrucks(); } catch (err) { setError(err.message); } finally { setLoading(false); } };
  useEffect(() => { refresh(); }, []);
  const filtered = useMemo(() => { const q = search.trim().toLowerCase(); return q ? trucks.filter((item) => [item.driver_name,item.truck_number,item.equipment_type,item.current_city,item.target_city,item.status].some((v)=>String(v||"").toLowerCase().includes(q))) : trucks; }, [trucks, search]);
  const save = async (payload) => { if (editing) await updateTruck(editing.id,payload); else await createTruck(payload); setEditing(null); setShowForm(false); };
  const remove = async (id) => { if (!window.confirm("Delete this truck?")) return; try { await deleteTruck(id); if (matchingId === id) { setMatchingId(null); setMatches([]); } } catch (err) { setError(err.message); } };
  const findMatches = async (truck) => { setMatchingId(truck.id); setMatches([]); setError(""); try { const {data}=await operationsApi.getTruckMatches(truck.id); setMatches(data?.matches||[]); } catch(err){ setError(getApiErrorMessage(err, "Unable to find matches for this truck.")); } };
  const accept = async (id) => { setAcceptingId(id); try { await acceptMatch(id); setMatches((c)=>c.map((m)=>m.id===id?{...m,status:"ACCEPTED"}:m)); await loadTrucks(); } catch(err){setError(err.message);} finally{setAcceptingId(null);} };
  return <div className="min-h-screen bg-[#f7f4ea]"><AppHeader title="TruckLink Driver" subtitle="Truck availability" actions={<button onClick={refresh} className="rounded-xl bg-[#2d6a4f] p-2.5 text-white"><RefreshCw className={`h-4 w-4 ${loading?"animate-spin":""}`}/></button>}/><main className="mx-auto max-w-7xl space-y-6 p-4 py-8 sm:px-6"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-widest text-[#2d6a4f]">Fleet availability</p><h1 className="text-3xl font-black text-[#203d31]">Truck management</h1><p className="mt-1 text-sm text-[#718078]">Register trucks, keep availability current, and find suitable loads.</p></div><button onClick={()=>{setEditing(null);setShowForm(true)}} className="inline-flex items-center justify-center gap-2 rounded-xl bg-[#2d6a4f] px-4 py-3 text-sm font-bold text-white"><Plus className="h-4 w-4"/> Register truck</button></div>{error&&<div className="rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</div>}{showForm&&<section className="rounded-3xl border border-[#e7dfca] bg-white p-6 shadow-sm"><h2 className="mb-5 text-xl font-bold text-[#29483a]">{editing?`Edit truck ${editing.truck_number}`:"Register truck"}</h2><TruckForm initialValue={editing} onSubmit={save} onCancel={()=>{setShowForm(false);setEditing(null)}}/></section>}<div className="relative"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-[#8aa89a]"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search driver, truck number, city, equipment..." className="w-full rounded-xl border-2 border-[#dde7e0] bg-white py-3 pl-10 pr-4 text-sm outline-none focus:border-[#2d6a4f]"/></div>{loading&&!trucks.length?<LoadingState label="Loading trucks..."/>:filtered.length?<div className="grid gap-4 lg:grid-cols-2">{filtered.map(truck=><article key={truck.id} className="rounded-2xl border border-[#e8f5ee] bg-white p-5 shadow-sm"><div className="flex justify-between gap-4"><div><div className="flex flex-wrap gap-2"><span className="rounded-full bg-[#e8f5ee] px-2.5 py-1 text-xs font-bold text-[#2d6a4f]">{truck.status}</span><span className="rounded-full bg-[#fff6df] px-2.5 py-1 text-xs font-bold text-[#8a6a2d]">{truck.equipment_type}</span></div><h2 className="mt-3 text-lg font-bold text-[#29483a]">{truck.truck_number}</h2><p className="text-sm text-[#718078]">{truck.driver_name}</p><p className="mt-1 text-xs text-[#8aa89a]">{truck.current_city}{truck.target_city?` → ${truck.target_city}`:""}</p></div><div className="text-right"><p className="font-black text-[#29483a]">{Number(truck.max_capacity_lbs||0).toLocaleString()} lbs</p><p className="text-xs text-[#8aa89a]">${Number(truck.min_rate_per_mile||0).toFixed(2)}/mi</p></div></div><div className="mt-4 flex flex-wrap gap-2"><button onClick={()=>findMatches(truck)} className="inline-flex items-center gap-1.5 rounded-lg bg-[#2d6a4f] px-3 py-2 text-xs font-bold text-white"><WandSparkles className="h-3.5 w-3.5"/> Find matches</button><button onClick={()=>{setEditing(truck);setShowForm(true);window.scrollTo({top:0,behavior:"smooth"})}} className="inline-flex items-center gap-1.5 rounded-lg border border-[#d9e5de] px-3 py-2 text-xs font-semibold text-[#4a6a5a]"><Edit3 className="h-3.5 w-3.5"/> Edit</button><button onClick={()=>remove(truck.id)} className="inline-flex items-center gap-1.5 rounded-lg bg-red-50 px-3 py-2 text-xs font-semibold text-red-600"><Trash2 className="h-3.5 w-3.5"/> Delete</button></div></article>)}</div>:<div className="rounded-2xl border border-[#e8f5ee] bg-white p-8 text-center text-sm text-[#718078]">No trucks found.</div>}{matchingId&&<section className="rounded-3xl border border-[#e7dfca] bg-[#fffdf8] p-6"><h2 className="text-xl font-bold text-[#29483a]">Recommended loads</h2><p className="mt-1 text-sm text-[#718078]">Review open loads that fit this truck’s equipment, capacity, location, and availability.</p><div className="mt-5 space-y-4">{matches.length?matches.map(match=><MatchCard key={match.id} match={match} onAccept={accept} accepting={acceptingId===match.id}/>):<p className="rounded-xl bg-white p-5 text-sm text-[#718078]">No qualifying loads found for this truck.</p>}</div></section>}</main></div>;
};
export default TrucksPage;
